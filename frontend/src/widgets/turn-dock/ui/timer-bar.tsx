/**
 * Re-exported at this path for `@/widgets/turn-dock`'s public API. The
 * implementation lives in `shared/ui/timer-bar.tsx` — see that file's doc
 * comment for why: `widgets/question-dock` renders the identical clock and
 * steiger forbids a cross-slice import between two sibling widgets.
 */
export { TimerBar } from "@/shared/ui";
